# evaluation-worker

Python GPU worker executing evaluation suites from `evaluation/` against
model bundles and immutable datasets, publishing evaluation reports as
immutable artifacts referenced by release manifests.

- Owner: ml-evaluation
