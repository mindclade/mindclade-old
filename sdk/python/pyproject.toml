[project]
name = "mindclade"
version = "0.0.0"
description = "Mindclade Python SDK"
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mindclade"]
