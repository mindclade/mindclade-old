# @mindclade Python SDK

Python client for the Mindclade platform under the `mindclade.*` namespace.
`src/` layout; released by semantic version independent of the monorepo.

- Owner: developer-experience
- Public surface is the typed high-level client; generated protocol modules
  are internal.
