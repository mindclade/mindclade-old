# @mindclade/sdk

TypeScript client for browser and server use. Strict TypeScript settings and
public-surface checks apply to releases; browser-safe entrypoints stay
separate from Node-only entrypoints.

- Owner: developer-experience
