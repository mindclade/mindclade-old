name: mirror-verification

on:
  schedule:
    - cron: "17 6 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify third_party source mirrors
        run: |
          echo "TODO: verify mirrored source digests against third_party/source_mirrors"
